# 项目总体进程

最后更新：2026-09-04

## 当前阶段

第一问“源域选择、特征构建与文件级跨载荷验证”已经完成本地封版，尚未进入第二问/第三问的迁移模型训练。

## 第一问已完成

- 完成 177 个 MAT 文件的目录、变量、采样率和 RPM 审计；
- 正式源域确定为全量 `48kHz_DE_data` 故障文件加 `48kHz_Normal_data`，共 56 个文件（N=4、B=12、IR=12、OR=28）；
- 48 kHz 源信号以抗混叠方式重采样至 32 kHz；默认窗口为 16384 点、hop 为 8192 点；
- 完成文件级 LOLO（逐折留出 0/1/2/3 hp）、特征消融、窗口敏感性、文件级 MI 和特征质量审计；
- 产出 26 维 Diagnostic 源域特征，以及同构 20 维 Source/Target Transfer 特征；目标 A–P 仅导出特征，不附加人工标签；
- 主 Logistic probe 的 20 维 Transfer 特征 LOLO：Macro-F1=0.875、Balanced Accuracy=0.902；这仅说明源域未见载荷下的诊断价值，不是目标域准确率。

## 数据质量结论与预处理决定

`outputs/q1/signal_quality_audit.csv` 已逐文件检查 177 条记录：全部可读取、有限值比例为 1、无常量/长零段、无疑似削顶、无精确重复文件。

8 条幅值分布提示均可由已知工况解释：12 kHz DE 的 B028（0028 故障尺寸，四载荷）、12 kHz FE 的 OR014@6（0014、Centered）以及 48 kHz DE 的 B021（0021，三个载荷）。它们是故障尺寸/位置的真实差异，不是损坏数据，保留用于后续跨工况泛化评估。

因此当前**不增加固定带通/陷波滤波器，也不删除数据**。保留去趋势、鲁棒特征计算和 48→32 kHz 抗混叠重采样；若第二问发现某一可验证的窄带干扰，再以独立消融实验评估针对性滤波。

## 主要交付物

- `src/q1_pipeline.py`：第一问完整可复现入口；
- `outputs/q1/validation/`：LOLO、消融与窗口敏感性结果；
- `outputs/q1/features_source_diagnostic.csv`：第二问输入；
- `outputs/q1/features_source_transfer.csv` 与 `features_target_transfer.csv`：第三问输入；
- `项目说明/第一问封版验证结果.md`：方法选择、实测结果与科学边界。

## 后续边界

下一阶段可开始第二问，但必须保持原始 MAT 文件为统计与验证单位；不能进行窗口级随机拆分，不能把 A–P 的候选结果报告为真值或准确率。
