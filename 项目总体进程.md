# 项目总体进程

更新日期：2026-09-06。本文件是当前项目的唯一进度入口。

| 问题 | 当前状态 | 正式入口 |
|---|---|---|
| 第一问 | 已封版 | `src/q1_pipeline.py`、`outputs/q1/`、`项目说明/第一问封版验证结果.md` |
| 第二问 | 主方法已确定，待干净重跑 | `src/q2_pipeline.py`（Diagnostic26 + MLP-only） |
| 第三问 | 未开始 | 后续冻结 Q2 MLP encoder 后，从零实现 POT Class-Regularized OT |
| 第四问 | 未开始 | 无 |

## 已冻结的边界

第一问只保留 Diagnostic26 正式路线。第二问唯一正式模型为 MLP：`26 → 128 → 64 → 32 → 4`，标签顺序固定为 `N/B/IR/OR`。验证必须以原始 MAT 文件为独立单位，保留 LOLO、内层文件级 Group CV、训练折内拟合 Scaler、类别/文件/窗口平衡采样和文件级概率聚合。

第二问旧的多模型比较、增强实验和历史输出已经清理；本次不重新运行第二问，因此当前不应把已删除的旧性能数字当作正式结论。

## 第三问计划与限制

第三问尚未实现。后续计划是冻结第二问的 MLP encoder，并使用 POT Class-Regularized OT 从零构建。未获得目标域真值前，不得报告目标域 accuracy、F1 或 recall，也不得用候选标签、参考答案或类别比例反向调参。

## 清理记录

2026-09-06 已删除旧 Transfer20/Transfer-v2、CORAL、DANN、Source-only、旧 Class-Reg OT、旧 Q3 预测与相关测试；Git 历史是唯一的回溯途径。
