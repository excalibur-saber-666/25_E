# 项目总体进程

更新日期：2026-09-06。本文件是当前项目的唯一进度入口。

| 问题 | 当前状态 | 正式入口 |
|---|---|---|
| 第一问 | 已封版 | `src/q1_pipeline.py`、`outputs/q1/`、`项目说明/第一问封版验证结果.md` |
| 第二问 | 已完成干净重跑 | `src/q2_pipeline.py`（Diagnostic26 + MLP-only，最终 seed=2025） |
| 第三问 | 已完成数值运行，待解释与写作 | `src/q3_pipeline.py`、`outputs/q3/`（POT Class-Regularized OT） |
| 第四问 | 未开始 | 无 |

## 已冻结的边界

第一问只保留 Diagnostic26 正式路线。第二问唯一正式模型为 MLP：`26 → 128 → 64 → 32 → 4`，标签顺序固定为 `N/B/IR/OR`。验证必须以原始 MAT 文件为独立单位，保留 LOLO、内层文件级 Group CV、训练折内拟合 Scaler、类别/文件/窗口平衡采样和文件级概率聚合。

第二问旧的多模型比较、增强实验和历史输出已经清理。当前正式全源导出由 seed=2025 生成；其 LOLO Macro-F1 为 0.936、BA 为 0.940，结果只描述源域跨载荷泛化。

## 第三问计划与限制

第三问已直接接入 PyPI 的 PythonOT/POT 0.9.7.post1：Diagnostic26 → 冻结 Q2 scaler/32D encoder → `ot.da.SinkhornLpl1Transport` → 线性头。正式参数固定为 `reg_e=0.1`、`reg_cl=1.0`，文件级质量按类别/文件平衡。A–P 的 16 条记录均为候选诊断，不得报告目标域 accuracy、F1 或 recall，也不得用候选标签、参考答案或类别比例反向调参。

正式数值设置固定为 `max_iter=10`、`max_inner_iter=20000`、`tol=1e-5`。这是在不改变 `reg_e=0.1` / `reg_cl=1.0`、不接触目标标签的前提下，为消除官方 POT 内层 Sinkhorn 数值告警所作的设置；`verification.json` 记录正式 600 rpm 的告警数为 0，570/600/630 rpm、LOTO、子采样和 surrogate 重拟合也均为 0。A–P 仍仅能称为候选诊断，数值收敛、MMD 降低和 surrogate 指标均不能证明目标域分类正确。

## 清理记录

2026-09-06 已删除旧 Transfer20/Transfer-v2、CORAL、DANN、Source-only、旧 Class-Reg OT、旧 Q3 预测与相关测试；Git 历史是唯一的回溯途径。随后从零建立当前的 POT 官方实现路线。
