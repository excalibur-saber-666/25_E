# 项目总体进程与交接

更新日期：2026-09-05。此文件是项目唯一的总体进程入口；历史的
`项目总体进程实现.txt` 已废止并删除。

## 当前状态

| 问题 | 状态 | 正式入口 |
|---|---|---|
| 第一问 | 已封版 | `outputs/q1/`、`项目说明/第一问封版验证结果.md` |
| 第二问 | 已封版 | `outputs/q2_refined/q2_refined_summary.md`、`项目说明/第二问深入完善交接.md` |
| 第三问 | 定向修正已完成，尚不可封版 | `outputs/q3_refined/q3_refined_summary.md`、`outputs/q3_refined/target_predictions_final.csv` |
| 第四问 | 暂不开始 | 必须先处理第三问 rpm/seed 不稳定候选，不能把候选标签当真值输入 |

## 已冻结的结论

第一问固定了 26D Diagnostic 特征和同构的 20D Transfer 特征。第二问以原始 MAT 文件为统计单位：Diagnostic26 MLP 的五种子 LOLO Macro-F1 为 `0.923 ± 0.018`，RF 为 `0.916 ± 0.016`；两者配对 bootstrap 区间包含零，不能写成 MLP 显著优于 RF。0 hp 是共同薄弱载荷，尤其要保留 IR Recall 降低这一限制。

第三问严格使用冻结的 Transfer20 和 `q2_transfer20_*` 接口，A–P 目标没有、也没有使用真值标签。CORAL 已修订为文件等权协方差与单次类别/文件加权，并补充公平 Source-linear、fold-specific CORAL 源域 LOLO、epsilon 与 Leave-One-Target-File-Out 稳定性。CORAL F1=`0.8315`，相对 Source-linear 的差值为 `-0.0459`，未越过 `-0.05` 工程负迁移线但接近该线。DANN 仍有严重 OR 偏置，继续只作失败对照。最终正式候选为 **CORAL**；这不是目标真实类别或目标准确率。

第三问文件级域差异为：Source MLP/linear MMD/PAD=`0.5334/1.8214`，CORAL=`0.1071/1.7500`，DANN=`0.2707/1.8036`。MMD/PAD 下降只表示域表征更难区分，不能证明目标分类正确。A–P 的逐文件候选、方法分歧、epsilon/LOTO/window/子采样一致性和复核标记均在 `outputs/q3/target_predictions_final.csv`。

## 第三问定向修正（Transfer-v2，2026-09-05）

原 Transfer20 固定 16384 点，在源域约对应 15 转、在目标域约对应 5 转，故不能作为跨转速物理同尺度表示。本轮没有修改冻结的 Diagnostic26、第一/二问正式模型或旧 `outputs/q3/`；另建 `src/q1_transfer_v2.py` 与 `src/q3_refined_pipeline.py`，结果仅写入 `outputs/q3_refined/`。

- Transfer-v2：每窗固定 8 个名义转数，重采样为 `256 samples/revolution × 8 = 2048`；这是只凭记录级 RPM 的**近似恒速角域重采样**，不是有转速脉冲支持的严格 order tracking。角域 Welch 分辨率固定为 `0.25 order`，并使用 500–2000、2000–4000、4000–8000 Hz 三段带通包络。
- 固定比较 S56 文件等权 CORAL、S56 类别/文件等权 CORAL、S16 平衡子集、raw-feature CORAL，并新增 5 个 source encoder（2025–2029）和 570/600/630 rpm 敏感性。目标 A–P 全程没有标签列，也没有用于任何拟合、选择或指标。
- S56 类别/文件等权 CORAL 的源域 LOLO Macro-F1=`0.9166`，比 Source-MLP=`0.9702` 低 `0.0536`，超过预设负迁移界线 `-0.05`，故不能作为主方法。
- 按预设回退规则运行 Class-Regularized OT（只用 source 标签作类一致性约束，目标无标签），其源域 LOLO Macro-F1=`0.9855`、BA=`0.9911`，因此当前唯一的最终**候选**方法为 `Class-Regularized OT Transfer-v2 S56`。这仍不是 A–P 准确率证据。
- 当前 A–P 候选类别分布为 N=1、B=1、IR=7、OR=7；最终表中仍有 11/16 需要复核（rpm ±5%、五 seed、低 margin/高 entropy 等）。所以第三问只能交付有边界的候选，**不得写“第三问封版”或开始第四问**。

交接时必须先阅读 `outputs/q3_refined/q3_refined_summary.md`、`verification.json`、`target_predictions_final.csv`、`rpm_sensitivity.csv`、`class_regularized_ot_seed_consensus.csv`、`source_retention.csv` 与 `target_class_geometry.csv`。严禁根据 PDF/参考答案、人为类别比例或候选标签来重新选择 rpm、seed 或方法；MMD/PAD/置信度只能描述域差异和内部稳定性。

## 已完成的质量边界

- 源域 56 个 MAT、目标域 16 个 MAT 是独立统计单位；窗口只用于训练及文件内部汇总，不能当作独立样本扩充。
- 每次目标文件类别都只是候选；禁止报告 A–P accuracy、F1、recall，禁止根据未知轴承几何强行命名 BPFO/BPFI/BSF。
- DANN LOLO 保持验证的每一折都在外层训练源文件上重训 source 初始化，未加载见过留出载荷的全源 Q2 模型；目标只以无标签输入参与域损失。
- 目标 softmax、MMD、PAD、PCA 可视化和 High/Medium/Review 只提供内部稳定性/域差异线索，不是正确概率。

## 第四问建议起点（第三问封版后才可执行）

1. 使用 `outputs/q3/target_predictions_final.csv` 的候选及 `review_required` 作为解释性分层，不把候选改成真值。
2. 载入 `embeddings_*`、`dann_training_history.csv`、`domain_metrics.csv` 与窗口级预测，解释 Source-only/CORAL/DANN 一致与分歧的文件。
3. 对重点 Review 文件优先做物理信号证据与敏感性分析；不得利用外部泄露标签反向调参。
4. 若要改进迁移模型，必须在独立方案中保留 Source-only/CORAL 对照、fold-specific source retention、类别塌缩和五 seed 稳定性检查；不能仅凭 MMD/PAD 或 softmax 选模型。

## 复现与审阅

```powershell
python .\src\q3_pipeline.py
python -m unittest discover -s tests -p "test_q2_*.py"
python -m unittest discover -s tests -p "test_q3_*.py"
```

第三问默认参数为 DANN 五种子 `2025–2029`、30 epochs；正式输出为 UTF-8-SIG CSV，图表为 PNG/SVG。不要使用 `git add .`：工作区可能保留第一问重生成图、旧实验输出及训练缓存，它们不属于当前正式交付。
