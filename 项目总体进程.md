# 项目总体进程与交接

更新日期：2026-09-06。此文件是项目唯一的总体进程入口；历史的
`项目总体进程实现.txt` 已废止并删除。

## 当前状态

| 问题 | 状态 | 正式入口 |
|---|---|---|
| 第一问 | 已封版 | `outputs/q1/`、`项目说明/第一问封版验证结果.md` |
| 第二问 | 已封版 | `outputs/q2_refined/q2_refined_summary.md`、`项目说明/第二问深入完善交接.md` |
| 第三问 | 方法学封版，候选结果保留复核层 | `outputs/q3_refined/q3_refined_summary.md`、`outputs/q3_refined/target_predictions_final.csv` |
| 第四问 | 可开始不确定性研究，不可把候选当真值 | 仅以候选、稳定性与物理解释证据分层 |

## 已冻结的结论

第一问固定了 26D Diagnostic 特征和同构的 20D Transfer 特征。第二问以原始 MAT 文件为统计单位：Diagnostic26 MLP 的五种子 LOLO Macro-F1 为 `0.923 ± 0.018`，RF 为 `0.916 ± 0.016`；两者配对 bootstrap 区间包含零，不能写成 MLP 显著优于 RF。0 hp 是共同薄弱载荷，尤其要保留 IR Recall 降低这一限制。

第三问严格使用冻结的 Transfer20 和 `q2_transfer20_*` 接口，A–P 目标没有、也没有使用真值标签。CORAL 已修订为文件等权协方差与单次类别/文件加权，并补充公平 Source-linear、fold-specific CORAL 源域 LOLO、epsilon 与 Leave-One-Target-File-Out 稳定性。CORAL F1=`0.8315`，相对 Source-linear 的差值为 `-0.0459`，未越过 `-0.05` 工程负迁移线但接近该线。DANN 仍有严重 OR 偏置，继续只作失败对照。最终正式候选为 **CORAL**；这不是目标真实类别或目标准确率。

第三问文件级域差异为：Source MLP/linear MMD/PAD=`0.5334/1.8214`，CORAL=`0.1071/1.7500`，DANN=`0.2707/1.8036`。MMD/PAD 下降只表示域表征更难区分，不能证明目标分类正确。A–P 的逐文件候选、方法分歧、epsilon/LOTO/window/子采样一致性和复核标记均在 `outputs/q3/target_predictions_final.csv`。

## 第三问封版前公平验证与解释链修正（Transfer-v2，2026-09-06）

原 Transfer20 固定 16384 点，在源域约对应 15 转、在目标域约对应 5 转，故不能作为跨转速物理同尺度表示。本轮没有修改冻结的 Diagnostic26、第一/二问正式模型或旧 `outputs/q3/`；另建 `src/q1_transfer_v2.py` 与 `src/q3_refined_pipeline.py`，结果仅写入 `outputs/q3_refined/`。

- Transfer-v2 现在有 30 个特征：删除了与 `angle_order_entropy` 完全重复的 `angle_psd_entropy`，并新增两个不依赖固定 Hz 带边的全频包络描述，以使 `full/no_absolute_hz/no_envelope/no_order` 四个消融集合严格不同。固定 Hz 包络依赖审计见 `fixed_hz_dependency_audit.md`；它可能携带 CWRU 设备共振，不能据此声称目标域正确。
- 建立了统一 surrogate UDA：每个 load 依次作为**无标签伪目标域**，其余三个 load 训练；每种方法预测完成后才连接留出标签评分。Source-only / CORAL / Class-Reg OT 的测试单位和划分完全一致，目标 A–P 标签与 PDF 从未出现于训练、适配或选择。
- 公平结果：Source-only F1=`0.9696±0.0526`、BA=`0.9702`；CORAL F1=`0.9422±0.0430`、BA=`0.9524`；Class-Reg OT 同为 F1=`0.9422±0.0430`、BA=`0.9524`，三者最差载荷 F1 都是 `0.8786`，均无类别塌缩。MMD 的下降不抵消适配方法在公平 surrogate 指标上的下降。
- 因 Source-only 以预先设定的 `0.01` F1 裕度超过两种适配方法，第三问唯一主方法改为 **Source-only Transfer-v2 S56**；CORAL 与 OT 保留为无监督对照/负结果，而不是为了“有迁移”而强行采用。这是基于公平 benchmark 的方法选择，不是 A–P 结果选择。
- 已重跑五个 seed、570/600/630 rpm、LOTO 和子采样。最终候选仍有 9/16 文件需要复核，故“方法学和验证协议”可以封版，但 A–P 只能作为带不确定性的候选诊断标签，不能当作第四问真值。

交接时必须先阅读 `outputs/q3_refined/q3_refined_summary.md`、`surrogate_uda_summary.csv`、`surrogate_uda_predictions.csv`、`verification.json`、`target_predictions_final.csv`、`method_target_stability.csv`、`fixed_hz_dependency_audit.md` 与 `target_class_geometry_final.csv`。严禁根据 PDF/参考答案、人为类别比例或候选标签重新选择 rpm、seed 或方法；MMD/PAD/置信度只能描述域差异和内部稳定性。

## 已完成的质量边界

- 源域 56 个 MAT、目标域 16 个 MAT 是独立统计单位；窗口只用于训练及文件内部汇总，不能当作独立样本扩充。
- 每次目标文件类别都只是候选；禁止报告 A–P accuracy、F1、recall，禁止根据未知轴承几何强行命名 BPFO/BPFI/BSF。
- DANN LOLO 保持验证的每一折都在外层训练源文件上重训 source 初始化，未加载见过留出载荷的全源 Q2 模型；目标只以无标签输入参与域损失。
- 目标 softmax、MMD、PAD、PCA 可视化和 High/Medium/Review 只提供内部稳定性/域差异线索，不是正确概率。

## 第四问建议起点（仅以不确定性分层为输入）

1. 使用 `outputs/q3_refined/target_predictions_final.csv` 的候选及 `review_required` 做解释性分层，不把候选改成真值。
2. 主研究线是“物理同尺度 Transfer-v2 + 源域诊断器 + 无标签目标不确定性审计”；CORAL/OT 是公平验证后未被采用的对照，而不是后续必须叠加的模块。
3. 对 Review 文件优先呈现窗口一致性、seed/rpm/LOTO 敏感性、最终方法空间几何与原始信号证据；不得利用外部泄露标签反向调参。
4. 如以后新增真实目标标注，必须另设独立测试集重评 Source-only/CORAL/OT；不能用当前 A–P 候选反过来验证或训练。

## 复现与审阅

```powershell
python .\src\q3_pipeline.py
python -m unittest discover -s tests -p "test_q2_*.py"
python -m unittest discover -s tests -p "test_q3_*.py"
```

第三问默认参数为 DANN 五种子 `2025–2029`、30 epochs；正式输出为 UTF-8-SIG CSV，图表为 PNG/SVG。不要使用 `git add .`：工作区可能保留第一问重生成图、旧实验输出及训练缓存，它们不属于当前正式交付。
