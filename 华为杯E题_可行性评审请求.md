# 2025 华为杯研究生数学建模竞赛 E 题：方案可行性评审请求

## 请评审者完成的事

请以“数学建模竞赛评委 + 跨域轴承故障诊断研究者”的视角，严格评审下面的方案，而不是默认认可。请重点回答：

1. 方案是否真正解决了题目的四项任务，而非仅完成源域分类？
2. 从约 1750 rpm 的 CWRU 源域迁移到约 600 rpm 的列车目标域，方法是否合理；有哪些明显的理论或工程漏洞？
3. 数据切分、特征、模型和评价是否可能产生数据泄漏、伪高精度或错误的目标域结论？
4. 哪些部分应删减，哪些部分应加强，才能在有限竞赛时间内形成一篇可信、有创新性、可复现的论文？
5. 请给出一份按优先级排序的“必须修改项 / 可选增强项 / 不建议做项”。

请特别指出任何不成立的假设。不要凭空给目标域 A-P 分配故障标签，也不要把无标签目标域的聚类结果称为“准确率”。

---

## 1. 题目与已核验数据事实

题目：**2025 年中国研究生数学建模竞赛 E 题——高速列车轴承智能故障诊断问题**。

题目要求依次完成：

1. 面向目标域的源域数据筛选、故障特征分析与特征提取；
2. 源域有监督故障诊断及评价；
3. 利用无标签目标域的迁移诊断，给出 A-P 文件的分类/标签和可视化分析；
4. 对迁移诊断的事前、迁移过程或事后可解释性进行分析。

### 1.1 源域

源域为 CWRU 试验台轴承振动数据，共 **161** 个 MATLAB `.mat` 文件：

| 类别 | 文件数 |
|---|---:|
| 正常 N | 4 |
| 外圈故障 OR | 77 |
| 内圈故障 IR | 40 |
| 滚动体故障 B | 40 |

采集分支如下：

| 分支 | B | IR | OR | N | 转速范围（rpm） |
|---|---:|---:|---:|---:|---:|
| 12 kHz DE | 16 | 16 | 28 | 0 | 1718-1797 |
| 12 kHz FE | 12 | 12 | 21 | 0 | 1728-1798 |
| 48 kHz DE | 12 | 12 | 28 | 0 | 1718-1797 |
| 48 kHz Normal | 0 | 0 | 0 | 4 | 约 1725-1796 |

- DE、FE、BA 分别表示驱动端、风扇端和基座加速度信号；本题源域文件的变量名不完全一致，需要根据变量后缀如 `_DE_time` 和 `RPM` 自动读取。
- 题面给出了 CWRU 轴承 SKF6205（DE）和 SKF6203（FE）的滚动体数量、直径、节径，因此可根据转频计算 BPFO、BPFI、BSF，用于验证源域机理特征。
- 源域存在明显类别不平衡，尤其正常原始文件仅 4 条；不能仅因滑窗后样本数量变大，就把数据称为“类别平衡”。

### 1.2 目标域

目标域为 A-P 共 **16** 个 `.mat` 文件。

- 每个文件只有一个和文件名相同的变量，例如 `A.mat` 中变量 `A`；形状均为 `256000 x 1`，数据类型为 `double`。
- 题面明确：采样率为 **32 kHz**，采集时长为 **8 s**，列车速度约 90 km/h，轴承转速约 **600 rpm**。
- 目标域同样仅包含 N、OR、IR、B 四种工作状态，但 A-P 的真实类别未提供。

已做的初步统计表明，16 个目标文件的 RMS 约为 1.46-31.52、Pearson 峭度约为 3.16-28.29、峰值因子约为 3.07-40.93。幅值尺度和冲击性差异很大，单一 RMS/阈值规则不可靠。

### 1.3 核心挑战

1. **转速失配**：源域约 1718-1798 rpm，目标域约 600 rpm；绝对频率位置不能直接对齐。
2. **设备与传感器失配**：CWRU 为电机试验台，目标为列车轴承；传感器位置、传递路径、噪声和振幅标定均可能不同。
3. **目标域完全无标签**：目标域无法计算真实分类准确率。
4. **源域类别不平衡且文件数有限**：随机切窗口再随机划分，极易把同一原始文件的相邻窗口放入训练、验证和测试集，造成严重泄漏。

---

## 2. 拟定主方案：机理引导的无监督域适应

### 2.1 源域筛选策略

先建立一个严格、可解释的平衡源域，以减少采样率、传感器和故障尺寸的混杂：

- 选 DE 通道；
- 选 48 kHz 分支并下采样到 32 kHz；
- 正常：`N_0` 至 `N_3`；
- 滚动体：`B007_0` 至 `B007_3`；
- 内圈：`IR007_0` 至 `IR007_3`；
- 外圈：`OR007@6_0` 至 `OR007@6_3`。

这得到 16 个原始文件、四类各 4 文件、均覆盖 0-3 载荷的受控起始源域。后续再扩展到 “48 kHz DE + 48 kHz normal” 的 56 个原始文件，并使用类别权重/均衡采样，作为鲁棒性和消融实验。

此处 DE 的选择是待检验假设：DE 通道靠近 CWRU 故障轴承、信号更清晰，但题面未说明列车传感器安装位置。因此拟通过无标签域距离比较（MMD、A-distance）以及源域文件级 Macro-F1，比较 DE 与 FE 候选源域；并不把 DE 的优越性当成先验事实。

### 2.2 预处理与样本构造

1. 源域 48 kHz 数据采用抗混叠重采样 `resample_poly(x, 2, 3)` 降至 32 kHz；目标域保持原始 32 kHz。
2. 去均值/去趋势，必要时进行带通滤波和 Hilbert 包络分析。
3. 对网络输入按窗口进行鲁棒归一化，例如 `(x - median(x)) / MAD(x)`；但同时保留未归一化的 RMS、峰峰值等统计量，供诊断解释与对照。
4. 建议窗口长度为 16,384 点、步长 8,192 点。目标域每窗约 0.512 s，覆盖约 5.12 个转子转动周期，适合低速冲击特征提取。
5. **任何训练、验证、测试划分都以原始 `.mat` 文件为 Group**。同一文件切出的窗口绝不跨集合。

待评审问题：16,384 点窗口是否合适？是否应采用基于已知转速的阶次重采样，使源、目标窗口含有相近的转数，而不是相同的时间长度？

### 2.3 特征体系

每个窗口生成三类特征：

1. **时域统计特征**：均值、标准差、RMS、偏度、峭度、峰峰值、峰值因子、脉冲因子、裕度因子等。
2. **机理频域特征**：Welch PSD、Hilbert 包络谱、谱峭度、分频带能量、包络谱峰值及其相对转频的阶次位置。
3. **时频特征**：STFT 对数谱或小波包能量。

源域中按题面给定参数计算：

\[
\mathrm{BPFO}=f_r\frac{N_d}{2}(1-\frac{d}{D}),\quad
\mathrm{BPFI}=f_r\frac{N_d}{2}(1+\frac{d}{D}),
\]

\[
\mathrm{BSF}=f_r\frac{D}{d}[1-(\frac{d}{D})^2].
\]

它们用于检查源域包络谱是否保留了合理的故障机理。由于目标轴承几何参数未知，**不将目标域某个谱峰直接命名为 BPFO/BPFI/BSF**；目标域重点使用相对转频的阶次结构、特征一致性和迁移模型结果。

### 2.4 模型与训练策略

先建立三个可比较的基线：

1. 手工特征 + SVM 或 LightGBM；
2. Source-only 的 WDCNN/ResNet1D；
3. CORAL 特征协方差对齐 + 分类器。

最终模型拟采用“两视图融合的 DANN + LMMD”：

```text
原始振动窗口 ── 1D WDCNN/ResNet ─┐
                                  ├─ 融合特征 ─ 四分类器（N/OR/IR/B）
机理特征向量 ── 小型 MLP ──────────┘
                                  └─ 梯度反转层 ─ 域分类器（源/目标）
                                  └─ LMMD 类条件分布对齐
```

损失函数：

\[
L=L_{\mathrm{cls}}^S+\lambda_dL_{\mathrm{domain}}
+\lambda_lL_{\mathrm{LMMD}}+\lambda_eH(p_T).
\]

- `L_cls^S`：带类别权重的源域四分类交叉熵；
- `L_domain`：DANN 梯度反转域对抗损失，用无标签目标窗口参与；
- `L_LMMD`：按类别的局部 MMD，对齐同类源/目标特征，目标类别由高置信伪标签给出；
- `H(p_T)`：目标预测熵的正则项。

训练拟分阶段进行：先 source-only 预训练；再逐步增加 DANN 系数；训练稳定后仅对置信度大于如 0.90 的目标窗口启用 LMMD 与伪标签，避免早期错误伪标签造成类别混淆。

待评审问题：在目标域只有 16 个长序列且窗口相关性强时，DANN + LMMD 是否过于复杂？是否应以 DANN 或 CORAL 作为最终主方法，并将 LMMD 作为增强/消融？

---

## 3. 评价与目标域输出原则

### 3.1 源域评价

源域必须按原始文件分组交叉验证或留文件验证，报告：

- Macro-F1、Balanced Accuracy、各类别 Recall；
- 按文件汇总的混淆矩阵，而不是仅按高度相关的窗口计算；
- 多随机种子平均值和标准差；
- 不同源域筛选方案、不同窗口长度和不同模型的消融比较。

### 3.2 无标签目标域评价

不得报告目标域“准确率”。可以报告：

- Source-only、CORAL、DANN、DANN+LMMD 的源/目标特征空间 t-SNE 或 UMAP；
- 域分类 A-distance、全局 MMD 和类别条件 MMD 的变化；
- 目标伪标签在不同随机种子、不同增强扰动下的一致率；
- 每个 A-P 文件的窗口级概率均值、投票类别、置信区间、低置信度标记；
- 与源域故障机理特征的对应证据。

最终提交表应包含 `文件编号、预测类别、平均置信度、窗口投票比例、关键机理特征、是否建议人工复核`。对于低置信度样本，保留“需复核”说明比强行高置信度分类更诚实。

---

## 4. 可解释性设计

| 阶段 | 解释手段 | 预期输出 |
|---|---|---|
| 事前 | BPFO/BPFI/BSF、包络谱、统计特征定义 | 为什么这些特征与三类故障机理相关 |
| 迁移过程 | t-SNE/UMAP、A-distance、MMD、类条件 MMD | 是否真的缩小了源/目标差异，是否发生类别混叠 |
| 事后 | SHAP（手工特征）、Integrated Gradients/Grad-CAM（1D CNN） | 哪些时段、包络频带或阶次推动了某类别判定 |

特别限制：目标域轴承几何参数未知时，解释中只能称“与源域某类故障的冲击/阶次模式相近”，不能把未知目标峰值伪装成已知的理论故障频率。

---

## 5. 软件与实现建议

主实现采用 Python 3.10+：

- 数据/信号：NumPy、SciPy、Pandas、PyWavelets；
- 传统模型：scikit-learn、LightGBM；
- 深度模型：PyTorch；
- 解释性：Captum、SHAP；
- 可视化：Matplotlib、Seaborn；
- MATLAB 仅作为 `.mat` 快速核验、包络谱和机理图的辅助工具。

建议模块化代码结构：

```text
src/
  data_loader.py          # 自动读取 CWRU 与 A-P mat 变量
  source_selection.py     # DE/FE、采样率、故障尺寸、MMD 筛选
  preprocessing.py        # 重采样、分窗、滤波、归一化
  features.py             # 时域、包络谱、时频特征
  models.py               # WDCNN/ResNet1D、特征融合
  losses.py               # GRL、DANN、LMMD/CORAL
  train_source.py
  train_uda.py
  evaluate.py
  explain.py
  predict_target.py       # 输出 A-P 预测表
```

---

## 6. 已核验的 GitHub 资源与授权边界

| 资源 | 用途 | 核验结果与使用建议 |
|---|---|---|
| https://github.com/thuml/Transfer-Learning-Library | DANN、DAN、CDAN、MCC 等 PyTorch 域适应部件 | MIT，代码完整。可直接复用算法组件，但须自行实现 1D 振动数据 loader 和 CNN backbone，并保留许可证。 |
| https://github.com/zhangjunzhao-00/DANN-Bearing-fault-diagnosis | 轴承 CWRU 的 DANN/GDANN 基线 | MIT，含真实模型、梯度反转层和训练代码；使用 TensorFlow/Keras，默认 10 分类，需改为 4 分类和本题 loader。可作为对照或迁移实现参考。 |
| https://github.com/xiaoyinya/A-Domain-Adaption-Transfer-Learning-Bearing-Fault-Diagnosis-Model-Based-on-Wide-Convolution-Deep-Neu | WDCNN-DANN 结构参考 | 有实际代码，但仓库未提供许可证；只能阅读理解，不能直接复制。 |
| https://github.com/kavianpour/DSAGCN-Bearing-Fault-Diagnosis | DANN + LMMD/图网络思路 | 文档有明确授权，但代码授权并不明确；可借鉴思路和实验设计，不应直接复制代码。 |
| https://github.com/jsdaiustc/Fault_frequency_learning | MATLAB 故障频率学习与稀疏信号处理 | 有 MATLAB 实现但未提供许可证；仅作为机理/方法参考。 |
| https://github.com/anahaaaa/bearing-fault-tl | 表面上是 CWRU 到另一目标域的迁移框架 | 虽为 MIT，但已核验 ResNet 文件只有 1 字节、DANN/CORAL/JMMD 目录为空，当前不可作为可运行基线。 |

### 外部数据的谨慎项

江南大学 JNU Bearing Dataset 有 600 rpm、800 rpm、1000 rpm 的四类数据，转速上很接近本题目标域，理论上可作为辅助公开源域。但目前检索到的 GitHub 镜像未给出明确许可证和原始数据授权说明；在没有核实原始数据出处与许可前，不把它纳入主方案。若使用任何额外公开数据，须按题面要求在论文中报告来源、链接、类别和具体用途。

---

## 7. 希望得到的结论格式

请给出以下结构化结论：

1. **总体可行性评分**：0-10 分，并说明理由；
2. **最严重的 3-5 个技术风险**；
3. **必须修改项**：应直接替换的步骤和推荐替代方案；
4. **建议保留的创新点**；
5. **可执行的最小可行方案（MVP）**：在不依赖额外公开数据的前提下，怎样用现有数据完成四问；
6. **增强版方案**：哪些实验值得在 MVP 成功后再加入；
7. **论文中绝对不能宣称的结论**，尤其是目标域无真实标签时的准确率、目标轴承未知几何参数下的故障频率归因等。

